1. 将文本大数据以适当的结构存入MySQL数据库；
    1. 获取数据清洗之后的好数据
    2. 入库
        1. 定义表结构
2. 在终端循环输入要查询的姓名，对开放记录进行查询；
    1. 实现精确查询和模糊查询(不加索引)；
    2. 实现精确查询和模糊查询(添加索引)--待实现；
    3. 实现内存-数据库的二级缓存策略，并显示每一次查询的时间消耗；
        1. 缓存查询基本实现思路
            1. 将查询的数据获取到缓存中，下一次查询先检查缓存中是否有数据，如果有，不再从mysql中获取
            2. 缓存不能存储太多数据，所以需要定期清理
            3. 考虑缓存清理规则，定义为、达到缓存上限之后，清理掉最早加入缓存的数据
    4. 实现redis-数据库的二级缓存策略，并显示每一次查询的时间消耗
